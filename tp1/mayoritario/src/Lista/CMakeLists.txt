set(SOURCE
        ${SOURCE}
        ${CMAKE_CURRENT_SOURCE_DIR}/lista_enteros.cpp
        ${CMAKE_CURRENT_SOURCE_DIR}/nodo_entero.cpp
        PARENT_SCOPE
        )
set(HEADERS
        ${HEADERS}
        ${CMAKE_CURRENT_SOURCE_DIR}/lista_enteros.h
        ${CMAKE_CURRENT_SOURCE_DIR}/nodo_entero.h
        PARENT_SCOPE
        )

